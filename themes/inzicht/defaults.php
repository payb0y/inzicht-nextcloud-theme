<?php

/**
 * In Zicht theme — visual styling only. Branding (name/logo/favicon) is
 * intentionally NOT overridden, so this OC_Theme is empty. Every getter in
 * OC_Defaults is guarded by method_exists(), so an empty class overrides nothing.
 */
class OC_Theme {
}
